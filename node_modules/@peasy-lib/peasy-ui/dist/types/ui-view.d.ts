import { UI } from "./ui";
import { UIAnimation } from "./ui-animation";
import { UIBinding } from "./ui-binding";
interface IUIViewOptions {
    parent?: UIView | UIBinding | null;
    prepare?: boolean;
    sibling?: UIView | Element | null;
    host?: Element;
}
export declare class UIView {
    state: 'created' | 'bound' | 'attaching' | 'attached' | 'rendered' | 'destroyed';
    parent: typeof UI | UIBinding | UIView;
    model: any;
    element: HTMLElement;
    host?: Element | null;
    bindings: UIBinding[];
    views: UIView[];
    render?: boolean;
    animations: UIAnimation[];
    animationQueue: UIAnimation[];
    destroyed: '' | 'queue' | 'destroy' | 'destroyed';
    moved: '' | 'queue' | 'move';
    attached: Promise<void>;
    private attachResolve;
    private parentElement;
    private sibling;
    static create(parent: HTMLElement, model: {} | undefined, template: HTMLElement | null, options?: IUIViewOptions): UIView;
    get lastElement(): Element;
    destroy(): void;
    terminate(): void;
    move(sibling: UIView | HTMLElement): void;
    play(animation: string | UIAnimation, element: HTMLElement): UIAnimation;
    updateFromUI(): void;
    updateToUI(): void;
    updateAtEvents(): void;
    updateAnimations(now: number): void;
    updateMove(): void;
    private getAnimations;
}
export {};
//# sourceMappingURL=ui-view.d.ts.map